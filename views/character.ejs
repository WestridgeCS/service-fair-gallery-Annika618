<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <main class="wrap">
    <a class="link" href="/">← Back</a>

    <section class="card">
      <h1><%= character.name %></h1>

      <p><span class="label">Type:</span> <%= character.type %></p>
      <p><span class="label">Trait:</span> <%= character.trait %></p>

      <div class="row">
        <a class="link" href="/characters/<%= character._id %>/edit">Edit</a>

        <form action="/characters/<%= character._id %>/delete" method="post" class="inline">
          <button class="danger" type="submit">Delete</button>
        </form>
      </div>
    </section>
  </main>
</body>
</html>
